name: admin
description: Painel Administrativo do Castanhal Hub.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.12.2

dependencies:
  flutter:
    sdk: flutter
  shared:
    path: ../shared
  provider: ^6.1.0
  http: ^1.2.0
  flutter_dotenv: ^5.2.1
  google_fonts: ^8.1.0
  file_picker: ^8.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/.env
